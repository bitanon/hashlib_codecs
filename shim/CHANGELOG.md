# 3.5.0

- **This package has been renamed to `convertlib`.**
- `hashlib_codecs` now re-exports `package:convertlib/convertlib.dart` with no
  API changes, so existing code keeps working. Please migrate your dependency
  and imports to `convertlib`.
- This package is **deprecated** and will be **_discontinued_** after a migration period.
